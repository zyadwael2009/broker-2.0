Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.6.0
Flask-Bcrypt==1.0.1
Flask-Cors==4.0.1
marshmallow==3.21.3
psycopg2-binary==2.9.9
python-dotenv==1.0.1
phonenumbers==8.13.43
gunicorn==22.0.0

# Phase 3 — photo processing + perceptual hashing for duplicate detection
Pillow==10.4.0
ImageHash==4.3.1

# Phase 6 — auth hardening
Flask-Limiter==3.8.0

# Phase 8 — deploy readiness
redis==5.0.8               # backend for Flask-Limiter and JWT revocation in prod
sentry-sdk[flask]==2.14.0  # error tracking; no-op when SENTRY_DSN unset

# Phase 12 — push notifications (FCM). Optional; the notifications module
# no-ops when FIREBASE_CREDENTIALS_JSON is unset so tests + local dev
# never need this to be installed successfully.
firebase-admin==6.5.0

# `flask seed-demo` uses this to download curated Unsplash demo photos.
# Not needed at runtime, but pinning it keeps `pip install` reproducible
# for anyone reseeding a fresh dev box.
requests==2.32.3
