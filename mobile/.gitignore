.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
.idea/
.vscode/

# Release signing — the keystore file itself and the password properties
# must NEVER be committed. See android/key.properties.example.
android/key.properties
*.jks
*.keystore
